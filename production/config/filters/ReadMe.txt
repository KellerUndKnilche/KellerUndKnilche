# Filterlisten landen hier als bad-words.txt
# nur ein Wort pro Zeile
# nur kleinbuchstaben
# scripts/dedupe-bad-words.sh kann für automatisierung verwendet werden (nur Linux)
        # chmod +x dedupe-bad-words.sh
        # ./dedupe-bad-words.sh
